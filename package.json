{
  "private": "true",
  "scripts": {
    "build": "pnpm run -r build",
    "format": "biome format --write",
    "lint": "biome check --fix",
    "prepare": "husky"
  },
  "dependencies": {
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@changesets/cli": "^2.27.7",
    "vitest": "^2.0.5"
  }
}
